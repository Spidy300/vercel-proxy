# HiAnime Vercel Proxy - Environment Variables

# Optional: Set your deployed proxy URL for proper M3U8 URL rewriting
# If not set, the proxy will auto-detect from request headers
# Example: https://your-proxy.vercel.app
PROXY_BASE_URL=

# Optional: Default referer for CDN requests
# Defaults to https://megacloud.tv if not set
DEFAULT_REFERER=https://megacloud.tv
